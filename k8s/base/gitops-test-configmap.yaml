# Ressource jetable pour vérifier le comportement "prune" d'ArgoCD (Phase 8) :
# créée ici, synchronisée, puis ce fichier est supprimé pour confirmer
# qu'ArgoCD retire automatiquement la ressource correspondante du cluster.
# À ne pas laisser en place après le test.
apiVersion: v1
kind: ConfigMap
metadata:
  name: gitops-prune-test
  namespace: billcom
data:
  purpose: "verify-argocd-prune"
